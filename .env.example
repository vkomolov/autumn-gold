# ================================
# 📘 Example environment variables
# ================================
# This file is just a reference template.
# It is NOT loaded by the application.
# The project already contains .env, which has all necessary public variables and included by git
# ================================
#!!! It is necessary to create your own `.env.local` file, which will be ignored by git:
#   cp .env.example .env.local
# Then fill in your actual values there.

# -------------------------------
# 🔒 Private / server-only variables
# -------------------------------
# These values MUST NOT be committed to git.
# Store them in `.env.local` or configure them on your hosting platform
# (e.g. Vercel → Settings → Environment Variables)

# Secret API key for internal server-side calls
API_SECRET_KEY=your-secret-key-here

# Mux credentials (example)
MUX_TOKEN_ID=your-mux-token-id
MUX_TOKEN_SECRET=your-mux-secret-key

# -------------------------------
# ⚙️ General application settings (optional)
# -------------------------------
# Logging level (optional): debug | info | warn | error
LOG_LEVEL=info

# Port number for local development (optional)
PORT=3000