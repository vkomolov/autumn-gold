[Scia Reto](https://sciareto.org) mind map   
> __version__=`1.1`,generatorId=`com.igormaznitsa:idea-mindmap:intellij-2024.17.0-WebStorm`
---

# layout\.tsx
> fillColor=`#000066`


## generateMetadata\(\):<br/>Promise\<Metadata\>
> align=`left`


### normalizeCMSPageMeta\(<br/>getCmsPageMetaDefault\(\),<br/>metaHandlers<br/>\)
> align=`left`,fillColor=`#00FF12`,mmd.emoticon=`application`

- NOTE
<pre>Создает MetaData для страницы
из данных TCmsPageMeta</pre>

#### getCmsPageMetaDefault\(\)
> align=`left`,fillColor=`#00FF12`,mmd.emoticon=`application`


##### mainPageMeta
> align=`left`,fillColor=`#808080`,mmd.emoticon=`barcode`

- NOTE
<pre>mockPageDataList[0]?.attributes.meta || {}</pre>

##### cmsPageMetaDefault
> fillColor=`#808080`,mmd.emoticon=`barcode`

- NOTE
<pre>TCmsPageMeta default</pre>

#### metaHandlers
> align=`left`,fillColor=`#00FF12`

- NOTE
<pre>Обработчики noIndex, noFollow, canonical,
alternate, og, twitter</pre>

## RootLayout
> align=`left`,fillColor=`#FF00FF`


### Header
> align=`left`,collapsed=`true`,fillColor=`#FF00FF`


#### hrefMailTo, <br/>email, <br/>\.\.\.rest
> align=`left`,collapsed=`true`,fillColor=`#808080`

- NOTE
<pre>returns Promise&lt;IContactsHeaderData&gt;

export interface IContactsHeaderData {
  hrefMailTo: string;
  hrefTelTo: string;
  telLabel: string;
  telAriaLabel: string;
  email: string;
  addressItems: string[];
}</pre>

##### getContactsHeaderData\(\)
> collapsed=`true`,fillColor=`#00FF12`,mmd.emoticon=`application`

- NOTE
<pre>MOCK CMS GETTERS to be replaced with real getters on fetching CMS Data
it is temporary used for async getting data from mock cms...
It returns Promise&lt;IContactsHeaderData | null&gt;</pre>

###### mockHeaderData\.contactsData
> fillColor=`#808080`,mmd.emoticon=`barcode`

- NOTE
<pre>export interface ICmsHeaderData {
  logoData?: {
    linkHref: string;
    imageData: TImageProps;
  };
  contactsData?: IContactsHeaderData;
}



const contactsData?: IContactsHeaderData = {
  hrefMailTo: &quot;mailto:agl@ag-landscape.com&quot;,
  hrefTelTo: &quot;tel:+13034679619&quot;,
  telLabel: &quot;303-467-9619&quot;,
  telAriaLabel: &quot;make a call to the master&quot;,
  email: &quot;agl@ag-landscape.com&quot;,
  addressItems: [&quot;4310 Youngfield St.&quot;, &quot;Wheat Ridge, CO&quot;],
};
</pre>

#### wrapperProps, <br/>imageProps
> align=`left`,collapsed=`true`,fillColor=`#808080`

- NOTE
<pre>The data, taken from the mock cms for building logo block in header...

export type TNavImageWrapperProps = {
  wrapperProps: TWrapperProps&lt;INavLinkProps&gt;; //href is required...
  imageProps: TImageProps;
};

export type TWrapperProps&lt;P&gt; = Omit&lt;P, &quot;style&quot;&gt; &amp; {
  style?: React.CSSProperties;
  [key: `data-${string}`]: string | undefined;
};

export interface INavLinkProps extends React.ComponentProps&lt;typeof Link&gt; {
  href: string;
  exact?: boolean; // exact path match or &quot;starts from ...&quot;
  className?: string;
  replace?: boolean;
  activeClassName?: string; //is used when &quot;active&quot;
}

// Final shape of image data (used in CMS, UI configs, etc.)
export type TImageProps = {
  src: TImageSource;
  alt: string;

  // Required width for container, aspect ratio will be calculated
  width: number;

  // Optional height — required if src is a string (not StaticImageData)
  height?: number;

  //objectFit?: React.CSSProperties['objectFit'];
  objectFit?: string; //objectFit is taken from imageProps.objectFit which is string

  // Responsive sizes
  breakPoints?: TImageSizes;

  // Allow passing any other native &lt;img&gt; or &lt;Image&gt; props if needed
  [key: string]: unknown;
};
</pre>

##### getNavImageWrapperProps\(\)
> collapsed=`true`,fillColor=`#00FF00`,mmd.emoticon=`application`

- NOTE
<pre>MOCK CMS GETTERS to be replaced with real getters on fetching CMS Data
it is temporary used for async getting data from mock cms...
It returns Promise&lt;TNavImageWrapperProps | null&gt;
</pre>

###### mockHeaderData\.logoData
> fillColor=`#808080`,mmd.emoticon=`barcode`

- NOTE
<pre>export interface ICmsHeaderData {
  logoData?: TCmsNavImageData;
  partnerData?: TCmsNavImageData;
  contactsData?: IContactsHeaderData;
  navMenuData?: THeaderNavMenuItem[];
}

export type TCmsNavImageData = {
  href: string;
  target?: string;
  ariaLabel: string;
  imageProps: TImageProps;
};

// Final shape of image data (used in CMS, UI configs, etc.)
export type TImageProps = {
  src: TImageSource;
  alt: string;

  // Required width for container, aspect ratio will be calculated
  width: number;

  // Optional height — required if src is a string (not StaticImageData)
  height?: number;

  //objectFit?: React.CSSProperties['objectFit'];
  objectFit?: string; //objectFit is taken from imageProps.objectFit which is string

  // Responsive sizes
  breakPoints?: TImageSizes;

  // Allow passing any other native &lt;img&gt; or &lt;Image&gt; props if needed
  [key: string]: unknown;
};</pre>

###### buildNavImageWrapperProps\(logoData\)
> fillColor=`#00FF12`,mmd.emoticon=`application`

- NOTE
<pre>It is used to handle the received CMS header data, or mock cms data
It returns TNavImageWrapperProps | null

props - type TCmsNavImageData = {
  href: string;
  target?: string;
  ariaLabel: string;
  imageProps: TImageProps;
};</pre>

#### navLinksData
> collapsed=`true`,fillColor=`#808080`

- NOTE
<pre>export type THeaderNavMenuItem = {
  id: string;
  label: string;
  href: string | null;
  parentId: string | null;
  order: number;
};

export type THeaderNavMenuNode = THeaderNavMenuItem &amp; {
  children?: THeaderNavMenuNode[];
};

navLinksData: THeaderNavMenuNode[]</pre>

##### getHeaderNavMenuTree\(\)
> collapsed=`true`,fillColor=`#00FF12`,mmd.emoticon=`application`

- NOTE
<pre>MOCK CMS GETTERS to be replaced with real getters on fetching CMS Data
it is temporary used for async getting data from mock cms...

It returns Promise&lt;THeaderNavMenuNode[] | null&gt;</pre>

###### mockHeaderData\.navMenuData
> fillColor=`#808080`,mmd.emoticon=`barcode`

- NOTE
<pre>THeaderNavMenuItem[]

export type THeaderNavMenuItem = {
  id: string;
  label: string;
  href: string | null;
  parentId: string | null;
  order: number;
};

</pre>

###### buildHeaderNavMenuTree\(navMenuData\)
> fillColor=`#00FF12`,mmd.emoticon=`application`

- NOTE
<pre>Builds a hierarchical navigation tree from a flat list of menu items.

Returns THeaderNavMenuNode[]

</pre>

#### \<LogoBlock<br/> wrapperProps=\{wrapperProps\}<br/> imageProps=\{imageProps\}<br/> hrefMailTo=\{hrefMailTo\}<br/>\>
> align=`left`,collapsed=`true`,fillColor=`#FF00FF`

- NOTE
<pre>function LogoBlock({
  wrapperProps,
  imageProps,
  hrefMailTo,
  children,
}: TLogoBlockProps)</pre>

##### \<ImageWrapper\<INavLinkProps\><br/> wrapper=\{NavLink\}<br/> wrapperProps=\{wrapperPropsStyled\}<br/> imageProps=\{imageProps\}<br/>/\>
> align=`left`,collapsed=`true`,fillColor=`#FF00FF`

- NOTE
<pre>wraps logo image with a custom Wrapper and returns Next Component Image, wrapped</pre>

###### wrapperStyle
> collapsed=`true`,fillColor=`#808080`

- NOTE
<pre>inline `style` object for an image wrapper

React.CSSProperties
</pre>

####### getImageWrapperStyle\(<br/>resolvedSrc, <br/>width, <br/>height, <br/>style<br/>\)
> align=`left`,collapsed=`true`,fillColor=`#00FF12`,mmd.emoticon=`application`

- NOTE
<pre>Build inline wrapper styles.
 - Includes relative positioning, width, aspect-ratio.
 - Preserves safe inline styles passed via propStyle (except restricted keys).

Generates a reliable inline `style` object for an image wrapper
that works seamlessly with `&lt;Image fill /&gt;` in Next.js.

returns React.CSSProperties</pre>

######## aspectRatio
> collapsed=`true`,fillColor=`#808080`


######### getAspectRatio\(<br/>width, <br/>src, <br/>height<br/>\)
> align=`left`,fillColor=`#00FF12`

- NOTE
<pre>Calculates the aspect ratio (as a string) to be used in inline styles like `aspectRatio`.

Supports two cases:

1. If `src` is a string (e.g. dynamic URL from CMS or API), both `width` and `height` must be provided manually.
2. If `src` is a StaticImageData object (e.g. from `import example from &quot;../image.jpg&quot;`),
the intrinsic dimensions from `src.width` and `src.height` are used.

If height is missing in case 1 or the StaticImageData object is malformed,
a warning is logged and the function returns `undefined`.

@returns The calculated aspect ratio as a string (e.g. &quot;3.0000&quot;), or `undefined` if it cannot be determined.</pre>

###### sizes
> collapsed=`true`,fillColor=`#808080`

- NOTE
<pre>responsive sizes attribute for the image if breakPoints are defined
sizes and imageProps.width are recommended to be in consistent units (px ↔ px, vw ↔ vw)</pre>

####### getImageSizes\(breakPoints\)
> collapsed=`true`,fillColor=`#00FF12`

- NOTE
<pre>Generate responsive sizes attribute if breakPoints are defined.
sizes and imageProps.width are recommended to be in consistent units (px ↔ px, vw ↔ vw)</pre>

######## normalizeBreakPoint\(<br/>key, value<br/>\)
> align=`left`,fillColor=`#00FF12`


###### \<Navlink\><br/>Image<br/>\</Navlink\>
> align=`left`,collapsed=`true`,fillColor=`#FF00FF`


####### \<link\><br/> Image<br/>\</Link\>
> align=`left`,fillColor=`#FF00FF`


##### \<IconTextLink<br/> href=\{hrefMailTo\}<br/> aria\-label=\{mailToAriaLabel\}<br/> tabIndex=\{0\}<br/> className="iconTextLink"<br/>\>
> align=`left`,collapsed=`true`,fillColor=`#FF00FF`

- NOTE
<pre>export default function IconTextLink({
  href = &quot;#&quot;,
  className,
  children,
  ...rest
}: IIconTextLinkProps)

</pre>

###### \<Link\><br/>children<br/>\</Link\>
> align=`left`,fillColor=`#FF00FF`


#### \<ContactsWrapper <br/>  data=\{rest\} <br/>/\>
> align=`left`,collapsed=`true`,fillColor=`#FF00FF`

- NOTE
<pre>export default async function ContactsWrapper({
  data,
}: {
  data: TContactsDataHeaderRest;
})

export interface IContactsHeaderData {
  hrefMailTo: string;
  mailToAriaLabel: string;
  hrefTelTo: string;
  telToAriaLabel: string;
  telLabel: string;
  email: string;
  addressItems: string[];
}


type TContactsDataHeaderRest = Omit&lt;
  IContactsHeaderData,
  &quot;hrefMailTo&quot; | &quot;email&quot; | &quot;mailToAriaLabel&quot;
&gt;;
</pre>

##### wrapperProps, <br/>imageProps
> align=`left`,collapsed=`true`,fillColor=`#808080`

- NOTE
<pre>The data, taken from the mock cms for building partners` block in header...

export type TNavImageWrapperProps = {
  wrapperProps: TWrapperProps&lt;INavLinkProps&gt;; //href is required...
  imageProps: TImageProps;
};

export type TWrapperProps&lt;P&gt; = Omit&lt;P, &quot;style&quot;&gt; &amp; {
  style?: React.CSSProperties;
  [key: `data-${string}`]: string | undefined;
};

export interface INavLinkProps extends React.ComponentProps&lt;typeof Link&gt; {
  href: string;
  exact?: boolean; // exact path match or &quot;starts from ...&quot;
  className?: string;
  replace?: boolean;
  activeClassName?: string; //is used when &quot;active&quot;
}

// Final shape of image data (used in CMS, UI configs, etc.)
export type TImageProps = {
  src: TImageSource;
  alt: string;

  // Required width for container, aspect ratio will be calculated
  width: number;

  // Optional height — required if src is a string (not StaticImageData)
  height?: number;

  //objectFit?: React.CSSProperties['objectFit'];
  objectFit?: string; //objectFit is taken from imageProps.objectFit which is string

  // Responsive sizes
  breakPoints?: TImageSizes;

  // Allow passing any other native &lt;img&gt; or &lt;Image&gt; props if needed
  [key: string]: unknown;
};
</pre>

###### getNavImageWrapperProps\(\)
> collapsed=`true`,fillColor=`#00FF00`,mmd.emoticon=`application`

- NOTE
<pre>MOCK CMS GETTERS to be replaced with real getters on fetching CMS Data
it is temporary used for async getting data from mock cms...
It returns Promise&lt;TNavImageWrapperProps | null&gt;
</pre>

####### mockHeaderData\.partnerData
> fillColor=`#808080`,mmd.emoticon=`barcode`

- NOTE
<pre>export interface ICmsHeaderData {
  logoData?: TCmsNavImageData;
  partnerData?: TCmsNavImageData;
  contactsData?: IContactsHeaderData;
  navMenuData?: THeaderNavMenuItem[];
}

export type TCmsNavImageData = {
  href: string;
  target?: string;
  ariaLabel: string;
  imageProps: TImageProps;
};

// Final shape of image data (used in CMS, UI configs, etc.)
export type TImageProps = {
  src: TImageSource;
  alt: string;

  // Required width for container, aspect ratio will be calculated
  width: number;

  // Optional height — required if src is a string (not StaticImageData)
  height?: number;

  //objectFit?: React.CSSProperties['objectFit'];
  objectFit?: string; //objectFit is taken from imageProps.objectFit which is string

  // Responsive sizes
  breakPoints?: TImageSizes;

  // Allow passing any other native &lt;img&gt; or &lt;Image&gt; props if needed
  [key: string]: unknown;
};</pre>

####### buildNavImageWrapperProps\(partnerData\)
> fillColor=`#00FF12`,mmd.emoticon=`application`

- NOTE
<pre>It is used to handle the received CMS header data, or mock cms data
It returns TNavImageWrapperProps | null

props - type TCmsNavImageData = {
  href: string;
  target?: string;
  ariaLabel: string;
  imageProps: TImageProps;
};</pre>

##### \<IconTextLink<br/> href=\{hrefTelTo\}<br/> aria\-label=\{telAriaLabel\}<br/> tabIndex=\{0\}<br/> className="iconTextLink"<br/>\>
> align=`left`,collapsed=`true`,fillColor=`#FF00FF`

- NOTE
<pre>/**
 * A reusable React component that renders a styled text link with an optional icon.
 * It automatically determines whether to use Next.js &lt;Link&gt; (for internal links)
 * or a regular &lt;a&gt; tag (for external links).
 *
 * @component
 * @param props - The component props.
 * @param props.href - The link target URL. Internal links start with '/'.
 * @param props.className - Optional additional class names.
 * @param props.children - The link content (text and/or icon).
 * @returns The rendered link element with proper styling and behavior.
 *
 * @example
 * &lt;IconTextLink href=&quot;/about&quot;&gt;
 *   &lt;Icon name=&quot;info&quot; /&gt; About Us
 * &lt;/IconTextLink&gt;
 */
export default function IconTextLink({
  href = &quot;#&quot;,
  className,
  children,
  ...rest
}: IIconTextLinkProps)</pre>

###### \<Link <br/> href=\{href\} <br/> className=\{\.\.\.\} <br/>\{\.\.\.rest\}\><br/>  \{children\}<br/>\</Link\>
> align=`left`,fillColor=`#FF00FF`


##### getSpans\(addressItems\)
> fillColor=`#00FF12`,mmd.emoticon=`application`


##### \<ImageWrapper\<INavLinkProps\><br/> wrapper=\{NavLink\}<br/> wrapperProps=\{wrapperProps\}<br/> imageProps=\{imageProps\}<br/>/\>
> align=`left`,collapsed=`true`,fillColor=`#FF00FF`

- NOTE
<pre>wraps logo image with a custom Wrapper and returns Next Component Image, wrapped
</pre>

###### wrapperStyle
> collapsed=`true`,fillColor=`#808080`

- NOTE
<pre>inline `style` object for an image wrapper

React.CSSProperties
</pre>

####### getImageWrapperStyle\(<br/>resolvedSrc, <br/>width, <br/>height, <br/>style<br/>\)
> align=`left`,collapsed=`true`,fillColor=`#00FF12`,mmd.emoticon=`application`

- NOTE
<pre>Build inline wrapper styles.
 - Includes relative positioning, width, aspect-ratio.
 - Preserves safe inline styles passed via propStyle (except restricted keys).

Generates a reliable inline `style` object for an image wrapper
that works seamlessly with `&lt;Image fill /&gt;` in Next.js.

returns React.CSSProperties</pre>

######## aspectRatio
> collapsed=`true`,fillColor=`#808080`


######### getAspectRatio\(<br/>width, <br/>src, <br/>height<br/>\)
> align=`left`,fillColor=`#00FF12`

- NOTE
<pre>Calculates the aspect ratio (as a string) to be used in inline styles like `aspectRatio`.

Supports two cases:

1. If `src` is a string (e.g. dynamic URL from CMS or API), both `width` and `height` must be provided manually.
2. If `src` is a StaticImageData object (e.g. from `import example from &quot;../image.jpg&quot;`),
the intrinsic dimensions from `src.width` and `src.height` are used.

If height is missing in case 1 or the StaticImageData object is malformed,
a warning is logged and the function returns `undefined`.

@returns The calculated aspect ratio as a string (e.g. &quot;3.0000&quot;), or `undefined` if it cannot be determined.</pre>

###### sizes
> collapsed=`true`,fillColor=`#808080`

- NOTE
<pre>responsive sizes attribute for the image if breakPoints are defined
sizes and imageProps.width are recommended to be in consistent units (px ↔ px, vw ↔ vw)</pre>

####### getImageSizes\(breakPoints\)
> collapsed=`true`,fillColor=`#00FF12`

- NOTE
<pre>Generate responsive sizes attribute if breakPoints are defined.
sizes and imageProps.width are recommended to be in consistent units (px ↔ px, vw ↔ vw)</pre>

######## normalizeBreakPoint\(<br/>key, value<br/>\)
> align=`left`,fillColor=`#00FF12`


###### \<Navlink\><br/>Image<br/>\</Navlink\>
> align=`left`,collapsed=`true`,fillColor=`#FF00FF`


####### \<Link\><br/> Image<br/>\</Link\>
> align=`left`,fillColor=`#FF00FF`


#### \<NavWrapper <br/>  data=\{navLinksData\} <br/>/\>
> align=`left`,collapsed=`true`,fillColor=`#FF00FF`

- NOTE
<pre>export default function NavWrapper({ data }: { data: THeaderNavMenuNode[] })

export type THeaderNavMenuItem = {
  id: string;
  label: string;
  href: string | null;
  parentId: string | null;
  order: number;
};

export type THeaderNavMenuNode = THeaderNavMenuItem &amp; {
  children?: THeaderNavMenuNode[];
};
</pre>

##### renderHeaderNavMenu\(<br/>data, <br/>stylesData<br/>\)
> align=`left`,collapsed=`true`,fillColor=`#00FF00`,mmd.emoticon=`application`

- NOTE
<pre>export const renderHeaderNavMenu = (
  data: THeaderNavMenuNode[],
  classNameData: TNavItemStyles,
): JSX.Element

</pre>

###### renderMenuItem\(<br/>item: THeaderNavMenuNode<br/>\): JSX\.Element | null
> align=`left`,collapsed=`true`,fillColor=`#00FF00`,mmd.emoticon=`application`

- NOTE
<pre>handles each navigation item within map cycles</pre>

####### \<Navlink\><br/> label<br/>\</Navlink\>
> align=`left`,collapsed=`true`,fillColor=`#FF00FF`

- NOTE
<pre>&lt;NavLink
 href={href}
 className={className}
 activeClassName={activeClassName}
 aria-label={`Go to ${label}`}
 tabIndex={0}
&gt;
 {label}
&lt;/NavLink&gt;</pre>

######## \<Link\><br/> label<br/>\</Link\>
> align=`left`,fillColor=`#FF00FF`


## App
> fillColor=`#FFC800`,leftSide=`true`,textColor=`#0000FF`


### \[\[\.\.\.page\]\]/<br/>page\.tsx
> fillColor=`#000066`,textColor=`#C0C0C0`


#### generateMetadata\(\):<br/>Promise\<Metadata\>
> align=`left`,fillColor=`#CCCCFF`


##### getPageHrefFromSlugParams\(<br/>page<br/>\)
> align=`left`,fillColor=`#00FF12`,mmd.emoticon=`application`

- NOTE
<pre>/**
 * Converts an array of dynamic route segments (slug parameters)
 * into a normalized page href path string.
 *
 * For example:
 *   [&quot;services&quot;, &quot;design-and-build&quot;] → &quot;/services/design-and-build&quot;
 *   undefined → &quot;/&quot;
 */
export const getPageHrefFromSlugParams = (page?: string[])</pre>

##### getCmsPageDataByHref\(<br/>pageHref<br/>\)
> align=`left`,fillColor=`#00FF12`,mmd.emoticon=`application`

- NOTE
<pre>making cache for each request with href path

export const getCmsPageDataByHref = async (
  pageHref: string,
  fakeDelay: number = 0,
): Promise&lt;IPageCms | undefined&gt;

export interface IPageCms {
  id: string;
  attributes: TPageCmsAttributes;

  sections: ISectionsItem[]; //rest page content
}

export type TPageCmsAttributes = {
  href: string; // &quot;&quot;, &quot;about&quot;, &quot;about/history&quot;
  label: string; //&quot;About&quot;,&quot;Services&quot; .... for navigation links
  meta: TCmsPageMeta;
  sitemapEntry: TSitemapEntry;
};</pre>

###### pageDataByHrefCache
> fillColor=`#808080`

- NOTE
<pre>Making cache with time validity

const pageDataByHrefCache = new Map&lt;string, IPageCms&gt;();</pre>

####### TTL\_MS
> fillColor=`#808080`

- NOTE
<pre>.env.local

NEXT_PUBLIC_TTL_MS=1800000

validity time period for the data to be refetched</pre>

##### normalizeCMSPageMeta\(<br/>meta, metaHandlers<br/>\)
> align=`left`,fillColor=`#00FF12`,mmd.emoticon=`application`

- NOTE
<pre>Создает MetaData для страницы
из данных TCmsPageMeta</pre>

#### Page\(<br/>\{ params \}: <br/>\{ params: TParams \}<br/>\)
> align=`left`,fillColor=`#FF00FF`,textColor=`#121212`


##### getPageHrefFromSlugParams\(<br/>page<br/>\)
> align=`left`,fillColor=`#00FF12`,mmd.emoticon=`application`,textColor=`#121212`

- NOTE
<pre>/**
 * Converts an array of dynamic route segments (slug parameters)
 * into a normalized page href path string.
 *
 * For example:
 *   [&quot;services&quot;, &quot;design-and-build&quot;] → &quot;/services/design-and-build&quot;
 *   undefined → &quot;/&quot;
 */
export const getPageHrefFromSlugParams = (page?: string[])</pre>

##### getCmsPageDataByHref\(<br/>pageHref<br/>\)
> align=`left`,fillColor=`#00FF12`,mmd.emoticon=`application`,textColor=`#121212`

- NOTE
<pre>making cache for each request with href path

export const getCmsPageDataByHref = async (
  pageHref: string,
  fakeDelay: number = 0,
): Promise&lt;IPageCms | undefined&gt;

export interface IPageCms {
  id: string;
  attributes: TPageCmsAttributes;

  sections: ISectionsItem[]; //rest page content
}

export type TPageCmsAttributes = {
  href: string; // &quot;&quot;, &quot;about&quot;, &quot;about/history&quot;
  label: string; //&quot;About&quot;,&quot;Services&quot; .... for navigation links
  meta: TCmsPageMeta;
  sitemapEntry: TSitemapEntry;
};</pre>

### NODE SCRIPTS
> fillColor=`#FFC800`,textColor=`#0000FF`


#### nodeCheck\.mjs
> fillColor=`#00FF12`,mmd.emoticon=`application`,textColor=`#121212`


#### processImages\.ts
> fillColor=`#00FF12`,mmd.emoticon=`application`,textColor=`#121212`


#### processVideos\.ts
> fillColor=`#00FF12`,mmd.emoticon=`application`,textColor=`#121212`

